{
  "name": "livemark",
  "version": "1.4.0",
  "description": "Live-updating Markdown viewer for macOS",
  "scripts": {
    "build:frontend": "rm -rf dist/frontend && mkdir -p dist/frontend/renderer && tsc --noEmit && esbuild src/renderer/renderer.ts --bundle --outfile=dist/frontend/renderer/renderer.js --platform=browser --target=safari13.1 && cp src/renderer/styles.css dist/frontend/renderer/ && cp index.html dist/frontend/",
    "build": "npm run build:frontend && cargo check --manifest-path src-tauri/Cargo.toml",
    "start": "tauri dev",
    "dev": "tauri dev",
    "test": "npm run build:frontend && cargo test --manifest-path src-tauri/Cargo.toml",
    "dist": "sh scripts/package-release.sh",
    "tauri": "tauri"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/turndown": "^5.0.6",
    "esbuild": "^0.27.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "highlight.js": "^11.9.0",
    "markdown-it": "^15.0.0",
    "turndown": "^7.2.2"
  }
}
